# Snowflake Multi-Database

The Solidatus Snowflake Multiple Database Connector is a Java based tool to combine and apply column lineage across multiple Snowflake databases. The connector joins together existing Solidatus Snowflake database models into a single model.
