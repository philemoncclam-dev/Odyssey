# Dependencies
node_modules/

# Build output
dist/
build/
# TanStack Router plugin scratch dir. routeTree.gen.ts IS tracked — it is
# generated but the app imports it, so a clean checkout has to build.
.tanstack/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Environment — never commit secrets. .env.example is the tracked template.
.env
.env.*
!.env.example
# Demo mode's flag file, committed on purpose: it turns on staged fixtures and
# holds nothing but a boolean. Anything needing a credential is not demo mode.
!app/.env.demo

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Test / coverage output
coverage/
*.log
